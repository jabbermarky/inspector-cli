import * as fs from "fs"; import { analyzeScriptSignals } from "./dist/ground-truth/signal-analysis.js"; const data = JSON.parse(fs.readFileSync("data/cms-analysis/mcsk6ly3-cmVkaGF0Lm.json", "utf8")); const signals = analyzeScriptSignals(data); console.log("Drupal script signals:", JSON.stringify(signals.filter(s => s.signal.includes("sites") || s.signal.includes("core") || s.signal.includes("modules")), null, 2));
